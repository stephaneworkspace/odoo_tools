/*use chrono::NaiveDate;

pub fn validator_date_aaaa_invoice(input: i32) -> Result<(), String> {
    match NaiveDate::from_ymd(input, 1, 1).and_hms(0, 0, 0) {
        Ok(_) => Ok(()),
        Err(err) => Err(format!("Error parse date: {}", err)),
    }
}
*/
